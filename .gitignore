# Secrets
config.env
.env
initial-passwords.txt

# Deploy state and logs (local to your machine)
.deploy-state
infra/deploy-*.log

# Runtime data
logs/
backups/
web/app/uploads/
web/app/languages/
product-photos/
backups/**/*.sql

# Composer vendor (rebuilt in Docker)
vendor/

# WordPress core (installed by Composer)
web/wp/

# Plugins (installed by Composer into named volume)
web/app/plugins/

# Local tooling
.mcp.json

# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp
*.bak

# Deploy-time runtime artifacts
.restore-done
website/

# StorageBox SSH key (per CLAUDE.md — must stay git-ignored)
config/backup/backup_key
