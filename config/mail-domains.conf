# Mail domains and mailboxes
# Used by: infra/setup-mailcow.sh (adds domains + mailboxes via Mailcow API)
# Used by: infra/dns-records.sh (outputs MX/SPF/DKIM/DMARC records)
#
# Format:
#   [domain]
#   localpart    Display Name
#
# Add a domain section, re-run setup-mailcow.sh (idempotent — skips existing).

[fraefel.de]
info        Fraefel Info

# PARKED — do not uncomment without a migration plan.
# These mailboxes were in scope for the original plan but deferred:
# - natursteindesign.de still lives on IONOS; migration is a separate session
#   (imapsync from imap.ionos.de plus optional PST supplement from PeterPC repo).
# - rechnung@fraefel.de has no recovery source identified yet.
#
# Uncommenting here will cause setup-mailcow.sh (incl. via deploy.sh --mode full)
# to provision them with fresh random passwords on next run. Coordinate with the
# mailbox owner before that happens, or their Outlook auth will break.
#
# rechnung    Fraefel Rechnung
#
# [natursteindesign.de]
# n.payan         N. Payan
# nathalie        Nathalie
# peter.fraefel   Peter Fraefel
